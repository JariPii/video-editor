import FilePicker from './FilePicker';
import VideoPlayer from './VideoPlayer';

const VideoEditor = () => {
  return (
    <div>
      <h1>VideoEditor</h1>
      <FilePicker />
      <VideoPlayer />
    </div>
  );
};

export default VideoEditor;
